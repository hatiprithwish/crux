node_modules
dist
build
.output
.wrangler
pnpm-lock.yaml

# Generated by drizzle-kit on every db:generate — reformatting it only creates churn, and the next
# generate undoes it.
apps/backend/src/db/migrations/meta

# Vendored by the shadcn CLI. CLAUDE.md forbids editing these by hand, so they can't be reformatted
# either — they're regenerated, not maintained.
apps/web/src/shadcn/ui
