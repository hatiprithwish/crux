vars {
  @description('''wrangler dev's port for this backend — check the terminal output, it varies if 8787 is already taken. Requires APP_ENV=local (see wrangler.jsonc top-level vars) so checkAuth bypasses real Clerk auth — no token needed.''')
  host: http://localhost:8787
}
