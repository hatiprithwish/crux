meta {
  name: Archive the timer tracker
  type: http
  seq: 7
}

delete {
  url: {{host}}/trackers/{{timeTrackerPublicId}}
  auth: none
}

tests {
  test("returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
