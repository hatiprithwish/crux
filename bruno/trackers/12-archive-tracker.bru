meta {
  name: Archive tracker
  type: http
  seq: 12
}

delete {
  url: {{host}}/trackers/{{trackerPublicId}}
  auth: none
}

tests {
  test("returns 200", function() {
    expect(res.status).to.equal(200);
  });

  test("isSuccess is true", function() {
    expect(res.body.isSuccess).to.equal(true);
  });
}
