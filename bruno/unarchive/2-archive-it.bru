meta {
  name: Archive it
  type: http
  seq: 2
}

delete {
  url: {{host}}/trackers/{{restoreTrackerPublicId}}
  auth: none
}

tests {
  test("returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
