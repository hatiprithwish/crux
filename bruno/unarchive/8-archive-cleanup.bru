meta {
  name: Archive it again (cleanup)
  type: http
  seq: 8
}

delete {
  url: {{host}}/trackers/{{restoreTrackerPublicId}}
  auth: none
}

tests {
  test("returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
